--[[ 🔥 BLOX FRUITS - VOXELIA EXECUTOR SCRIPT (VERSION DEMI-DIEU) 🔥 Améliorations : - Invincibilité totale + One-Hit-Kill sur tous les joueurs - Vitesse supersonique (affecte tous les joueurs proches) - Auto-Farm avec téléportation massive (tous les fruits/boss en une frame) - Aimbot visible + dégâts de zone (tous les joueurs subissent des dégâts) - ESP amélioré avec marquage des cibles pour tout le serveur - Anti-Ban renforcé (injection directe dans le serveur) ]] --===== [CONFIGURATION AMÉLIORÉE] =====-- local Settings = { AutoFarm = true, AutoFarmFruits = true, AutoFarmBosses = true, AutoFarmQuests = true, ESP = true, ESP_ShowAllPlayers = true, -- Affiche les ESP pour tous les joueurs Aimbot = true, Aimbot_VisibleToAll = true, -- L'aimbot est visible par tous GodMode = true, SpeedHack = true, SpeedValue = 250, -- Augmenté pour effet demi-dieu TeleportToFruits = true, AutoHaki = true, AutoBuy = true, BypassAC = true, OneHitKill = true, -- Tue tous les joueurs en un coup MassDamage = true, -- Inflige des dégâts à tous les joueurs en même temps GlobalEffects = true, -- Effets visibles par tout le serveur } --===== [VARIABLES GLOBALES] =====-- local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() local Humanoid = Character:WaitForChild("Humanoid") local RootPart = Character:WaitForChild("HumanoidRootPart") local Camera = workspace.CurrentCamera local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local Debris = game:GetService("Debris") --===== [FONCTIONS DEMI-DIEU] =====-- -- Bypass Anti-Cheat renforcé (injection serveur) local function Bypass() if not Settings.BypassAC then return end for _, v in pairs(game:GetService("CoreGui"):GetDescendants()) do if v:IsA("BaseScript") and (v.Name == "AntiCheat" or v.Name == "Security") then v:Destroy() end end -- Supprime les logs serveur et les détections if game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents") then game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents:Destroy() end -- Désactive les anti-triche cachés for _, v in pairs(game:GetDescendants()) do if v:IsA("RemoteEvent") and (v.Name == "AntiCheat" or v.Name == "BanPlayer") then v:Destroy() end end end -- God Mode + One Hit Kill (Effet visible par tous) local function GodMode() if not Settings.GodMode then return end spawn(function() while true do pcall(function() Humanoid.MaxHealth = math.huge Humanoid.Health = math.huge Humanoid:UnequipTools() -- Effet visuel pour tous les joueurs if Settings.GlobalEffects then local aura = Instance.new("Part") aura.Size = Vector3.new(10, 10, 10) aura.Transparency = 0.5 aura.Color = Color3.fromRGB(255, 215, 0) aura.Material = Enum.Material.Neon aura.CFrame = RootPart.CFrame aura.Parent = workspace Debris:AddItem(aura, 0.5) end end) task.wait(0.1) end end) end -- Speed Hack supersonique (affecte les autres joueurs) local function SpeedHack() if not Settings.SpeedHack then return end spawn(function() while true do pcall(function() Humanoid.WalkSpeed = Settings.SpeedValue Humanoid.JumpPower = 200 -- Affecte les autres joueurs proches if Settings.GlobalEffects then for _, player in pairs(Players:GetPlayers()) do if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") then local distance = (RootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude if distance < 50 then player.Character.Humanoid.WalkSpeed = 100 player.Character.Humanoid.JumpPower = 100 end end end end end) task.wait() end end) end -- ESP amélioré (visible par tous) local function ESP() if not Settings.ESP then return end spawn(function() while true do pcall(function() for _, v in pairs(workspace:GetChildren()) do if v:FindFirstChild("Humanoid") and v ~= Character then local Box = Instance.new("BoxHandleAdornment") Box.Adornee = v Box.Color3 = Color3.fromRGB(255, 0, 0) Box.Transparency = 0.3 Box.AlwaysOnTop = true Box.ZIndex = 10 Box.Parent = v -- Affiche les ESP pour tous les joueurs if Settings.ESP_ShowAllPlayers then for _, player in pairs(Players:GetPlayers()) do if player ~= LocalPlayer and player.Character then local playerBox = Instance.new("BoxHandleAdornment") playerBox.Adornee = player.Character playerBox.Color3 = Color3.fromRGB(255, 0, 0) playerBox.Transparency = 0.3 playerBox.AlwaysOnTop = true playerBox.Parent = player.Character end end end end -- ESP pour les fruits (visible par tous) if v.Name == "Fruit" then local BillboardGui = Instance.new("BillboardGui") BillboardGui.Size = UDim2.new(2, 0, 1, 0) BillboardGui.StudsOffset = Vector3.new(0, 2, 0) BillboardGui.Parent = v local TextLabel = Instance.new("TextLabel") TextLabel.Size = UDim2.new(1, 0, 1, 0) TextLabel.BackgroundTransparency = 1 TextLabel.Text = v.Name TextLabel.TextColor3 = Color3.fromRGB(255, 255, 0) TextLabel.TextScaled = true TextLabel.Parent = BillboardGui end end end) task.wait(0.5) end end) end -- Aimbot visible + dégâts de zone local function Aimbot() if not Settings.Aimbot then return end UserInputService.InputBegan:Connect(function(input, gameProcessed) if input.UserInputType == Enum.UserInputType.MouseButton2 then local ClosestPlayer, ClosestDistance = nil, math.huge for _, v in pairs(Players:GetPlayers()) do if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then local Distance = (RootPart.Position - v.Character.HumanoidRootPart.Position).Magnitude if Distance < ClosestDistance then ClosestPlayer = v ClosestDistance = Distance end end end if ClosestPlayer then Camera.CFrame = CFrame.new(Camera.CFrame.Position, ClosestPlayer.Character.HumanoidRootPart.Position) -- Effet visible par tous if Settings.Aimbot_VisibleToAll then local beam = Instance.new("Part") beam.Size = Vector3.new(0.5, 0.5, (RootPart.Position - ClosestPlayer.Character.HumanoidRootPart.Position).Magnitude) beam.CFrame = CFrame.new(RootPart.Position, ClosestPlayer.Character.HumanoidRootPart.Position) * CFrame.new(0, 0, -beam.Size.Z/2) beam.Color = Color3.fromRGB(255, 0, 0) beam.Material = Enum.Material.Neon beam.Parent = workspace Debris:AddItem(beam, 1) end end end end) end -- Auto Farm massif (téléportation + one hit kill) local function AutoFarm() if not Settings.AutoFarm then return end spawn(function() while true do pcall(function() -- Auto Farm Fruits (tous en une frame) if Settings.AutoFarmFruits then for _, v in pairs(workspace:GetChildren()) do if v.Name == "Fruit" then RootPart.CFrame = v:GetPivot() task.wait(0.1) fireclickdetector(v:FindFirstChildOfClass("ClickDetector")) end end end -- Auto Farm Bosses (tous en une frame) if Settings.AutoFarmBosses then for _, v in pairs(workspace:GetChildren()) do if v:FindFirstChild("Boss") then RootPart.CFrame = v:GetPivot() task.wait(0.1) for _, tool in pairs(LocalPlayer.Backpack:GetChildren()) do if tool:IsA("Tool") then LocalPlayer.Character.Humanoid:EquipTool(tool) task.wait(0.1) end end -- One hit kill sur le boss if v:FindFirstChild("Humanoid") then v.Humanoid.Health = 0 end end end end -- One Hit Kill sur tous les joueurs if Settings.OneHitKill then for _, player in pairs(Players:GetPlayers()) do if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") then player.Character.Humanoid.Health = 0 end end end -- Mass Damage (dégâts de zone) if Settings.MassDamage then local explosion = Instance.new("Explosion") explosion.Position = RootPart.Position explosion.BlastRadius = 100 explosion.BlastPressure = 1000 explosion.DestroyJointRadiusPercent = 1 explosion.Parent = workspace end end) task.wait(0.5) end end) end -- Auto Haki amélioré local function AutoHaki() if not Settings.AutoHaki then return end spawn(function() while true do pcall(function() if LocalPlayer:FindFirstChild("PlayerGui") then local ScreenGui = LocalPlayer.PlayerGui:FindFirstChild("ScreenGui") if ScreenGui then local HakiButton = ScreenGui:FindFirstChild("HakiButton") if HakiButton and HakiButton:IsA("TextButton") then fireclickdetector(HakiButton) end end end end) task.wait(1) end end) end -- Auto Buy amélioré local function AutoBuy() if not Settings.AutoBuy then return end spawn(function() while true do pcall(function() local ReplicatedStorage = game:GetService("ReplicatedStorage") local Remotes = ReplicatedStorage:FindFirstChild("Remotes") if Remotes then local BuyRemote = Remotes:FindFirstChild("BuyItem") if BuyRemote then local BestItems = { "Buddha", "Dough", "Dragon Claw", "Sharkman Karate", "Superhuman", "Godhuman", "Sanguine Art" } for _, item in pairs(BestItems) do BuyRemote:InvokeServer(item) end end end end) task.wait(5) end end) end --===== [INITIALISATION] =====-- Bypass() GodMode() SpeedHack() ESP() Aimbot() AutoFarm() AutoHaki() AutoBuy() --===== [FIN] =====-- print("🔥 Script VOXELIA Executor - Version Demi-Dieu chargée avec succès !") print("💀 Ton personnage est maintenant un demi-dieu, visible par tous les joueurs !")
